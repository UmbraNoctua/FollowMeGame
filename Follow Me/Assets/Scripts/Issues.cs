using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Issues : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
//this script is to detail issues I am currently having.
//1. camera no longer follows player after setting it's bounds to the games screen
//2. coded run movement for player so that when they holds shift and w,s,a,d they run. 
//2 continued. however code havin a hissy fit about something i wrote
//3. button images do not show up even though I applied them, so for now added rough temp text.

//non issues that can be focused on later.
//1. creating pause screen transition- Elliot knows how to do it but I do not...